# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
dist-ssr/
target/
*.local
/gen/schemas

# ── Logs ──
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Credentials / config (MUST NOT commit) ──
Mihoyo-env.json
runtime.json
env.json
*.env

# ── Worktrees (isolated dev environments) ──
.worktrees/

# ── Local agent skills (Hermes/Claude Code symlinks, worktree local only) ──
.agents/

# ── Superpowers local notes (not for public repository) ──
# 设计文档在 docs/specs/（已统一），本地缓存的 skill 不提交
.claude/plugins/

# ── Reference materials (vendor docs, API collections) ──
refs/

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor (allow shared config) ──
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
